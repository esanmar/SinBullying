# ===========================================
# VARIABLES DE ENTORNO - SinBullying Platform
# ===========================================
# Copia este archivo a .env.local para desarrollo local
# En producción (Vercel), configura estas variables en el dashboard

# ===========================================
# VERCEL POSTGRES (Base de Datos)
# ===========================================
# Estas variables se crean automáticamente al añadir Vercel Postgres
# No necesitas configurarlas manualmente
POSTGRES_URL=
POSTGRES_PRISMA_URL=
POSTGRES_URL_NON_POOLING=
POSTGRES_USER=
POSTGRES_HOST=
POSTGRES_PASSWORD=
POSTGRES_DATABASE=

# ===========================================
# VERCEL BLOB STORAGE (Almacenamiento de Archivos)
# ===========================================
# Token para Vercel Blob Storage (10GB incluidos en plan Hobby)
# Se genera automáticamente al crear un Blob Store en Vercel
BLOB_READ_WRITE_TOKEN=

# ===========================================
# SMTP (Notificaciones por Email)
# ===========================================
# Configuración SMTP para enviar notificaciones
# Puedes usar Gmail, Outlook, tu servidor SMTP, etc.

# Servidor SMTP (ejemplos):
# - Gmail: smtp.gmail.com
# - Outlook: smtp-mail.outlook.com
# - Yahoo: smtp.mail.yahoo.com
# - Tu propio servidor: mail.tudominio.com
SMTP_HOST=smtp.gmail.com

# Puerto SMTP (común):
# - 587 (TLS/STARTTLS) - Recomendado
# - 465 (SSL)
# - 25 (Sin cifrado, no recomendado)
SMTP_PORT=587

# Usar SSL/TLS (true para puerto 465, false para 587)
SMTP_SECURE=false

# Usuario SMTP (tu email completo)
SMTP_USER=tu-email@gmail.com

# Contraseña SMTP
# IMPORTANTE: Para Gmail, usa una "Contraseña de Aplicación"
# No uses tu contraseña normal de Gmail
# Crea una en: https://myaccount.google.com/apppasswords
SMTP_PASS=tu-contraseña-de-aplicacion

# Email remitente (opcional, por defecto usa SMTP_USER)
# Formato: "Nombre" <email@dominio.com>
SMTP_FROM="SinBullying Alertas" <tu-email@gmail.com>

# Email del administrador que recibirá las alertas
ADMIN_EMAIL=director@escuela.edu

# ===========================================
# OPCIONAL: Google Cloud Storage (Legacy)
# ===========================================
# Solo necesario si prefieres usar Google Cloud Storage
# en lugar de Vercel Blob Storage
# GOOGLE_PROJECT_ID=
# GOOGLE_CLIENT_EMAIL=
# GOOGLE_PRIVATE_KEY=
# GOOGLE_BUCKET_NAME=

# ===========================================
# DESARROLLO LOCAL
# ===========================================
# Para desarrollo local, ejecuta:
# 1. npm install -g vercel
# 2. vercel link (vincula tu proyecto)
# 3. vercel env pull .env.local (descarga las variables)
# 4. npm run dev

# ===========================================
# GUÍA RÁPIDA: CONFIGURAR GMAIL
# ===========================================
# 1. Ve a tu cuenta de Google: https://myaccount.google.com
# 2. Seguridad > Verificación en dos pasos (actívala si no lo está)
# 3. Seguridad > Contraseñas de aplicaciones
# 4. Selecciona "Correo" y "Otro (nombre personalizado)"
# 5. Escribe "SinBullying" y genera
# 6. Copia la contraseña de 16 caracteres
# 7. Úsala en SMTP_PASS

# Variables:
# SMTP_HOST=smtp.gmail.com
# SMTP_PORT=587
# SMTP_SECURE=false
# SMTP_USER=tu-email@gmail.com
# SMTP_PASS=abcd efgh ijkl mnop (la contraseña de aplicación)
# SMTP_FROM="SinBullying Alertas" <tu-email@gmail.com>
# ADMIN_EMAIL=director@escuela.edu
